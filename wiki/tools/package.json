{
  "name": "wiki-tools",
  "private": true,
  "type": "module",
  "description": "Zero-dependency TypeScript toolchain for the study wiki. Run directly with Node 24+ (type stripping) — no npm install, ever.",
  "scripts": {
    "build": "node build.ts",
    "check": "node check.ts",
    "serve": "node serve.ts",
    "browser": "tsc -p browser",
    "typecheck": "tsc --noEmit -p ."
  }
}
